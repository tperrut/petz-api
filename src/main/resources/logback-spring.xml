<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <Pattern>%yellow[%d{yyyy-MM-dd_HH:mm:ss.SSS}] [%thread] [%-5level] %logger{15} - %msg%n</Pattern>
            
        </encoder>
    </appender>s

    <springProfile name="test">
        <logger name="org.springframework" level="info" additivity="false">
            <appender-ref ref="STDOUT" />
        </logger>

        <logger name="com.pets.api" level="debug" additivity="false">
            <appender-ref ref="STDOUT" />
        </logger>

        <root level="error">
            <appender-ref ref="STDOUT" />
        </root>
    </springProfile>

    <springProfile name="dev,docker">
        <timestamp key="executionDate" datePattern="yyyy-MM-dd"/>

        <appender name="FILE" class="ch.qos.logback.core.FileAppender">
            <file>./logs/app-${executionDate}.log</file>
            <append>true</append>
            <encoder>
                <pattern>[yellow%d{yyyy-MM-dd HH:mm:ss.S}] blue%thread [%logger{36}] %level: %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="org.springframework" level="info" additivity="false">
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </logger>

        <logger name="com.pets.api" level="debug" additivity="false">
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </logger>

        <root level="error">
            <appender-ref ref="STDOUT" />
            <appender-ref ref="FILE" />
        </root>
    </springProfile>



</configuration>